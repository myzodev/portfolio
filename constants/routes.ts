export const HOME_ROUTE = "/";

export const CONTACT_ROUTE = "/contact";

export const PROJECTS_ROUTE = "/projects";

export const PROJECTS_ITEM_ROUTE = (projectId: string) => `${PROJECTS_ROUTE}/${projectId}`;

export const LIGHT_ROUTES = [CONTACT_ROUTE];
