---
import { getLangFromUrl } from '@/i18n/utils';

const lang = getLangFromUrl(Astro.url);
const href = lang === 'en' ? '/sk' : '/'
const label = lang === 'en' ? 'SK' : 'EN'
---
<a class="navbar-menu-link" href={`${href}`} data-astro-reload>{label}</a>